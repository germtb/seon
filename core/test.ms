import { reduce } from './list'

// describe = (message, tests) => reduce((acc, ))

// test = (message, testFunction) => let result = testFunction() in match result
//   | Pass -> log(message + 'passed')
//   | Failed -> log(message + 'failed with error: ' + result.error)

module = {
  // test
}
